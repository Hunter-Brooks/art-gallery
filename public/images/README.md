Drop your pixel cat image here as `pixel-cat.png`.

This asset is referenced by `Gallery.js` for the secret easter‑egg animation.
You can use the attached cat picture or any other small pixel art; the file should
be 64×64 (or similar) and named `pixel-cat.png`.

Additional images (e.g. a tiny transparent placeholder) can also go in this
folder if needed.